import PageCard from "@/components/global/pageCard";
import DefaultLayout from "@/layouts/DefaultLayout";

const Edit = () => {
  return (
    <DefaultLayout currentPage={"settings"} subPage={"edit"}>
      <PageCard>edit 작성중</PageCard>
    </DefaultLayout>
  );
};

export default Edit;
